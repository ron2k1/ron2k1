# Profile README redesign: comic issue, Connect Four vs. the bot, toolbelt

Date: 2026-09-17. Repo: `ron2k1/ron2k1` (profile README). Approved in chat on 2026-09-17: Connect
Four against a self-written bot, ClawWorld removed, copy cut to the owner's voice with little
explaining.

## Goals

1. The README reads as the same identity as `ron2k1.github.io`: the comic-issue design (paper,
   ink, red, blue, yellow; Bangers display type; JetBrains Mono labels; hard offset shadows;
   halftone dots).
2. A playable game on the page: Connect Four, visitors vs. a bot written in this repo.
3. Skills shown as a drawn toolbelt that mirrors the resume's four skill groups, plus four stat
   panels with real numbers.
4. Copy in the owner's voice: first person, short, plain, no explanatory scaffolding. The work
   speaks for itself.

## Non-goals

- None of the six template tells: capsule-render banners, shields.io badge walls, komarev view
  counters, the Platane contribution snake, `<div align="center">` wrappers, raw `<table>` for spec
  rows.
- No third-party image or stats service. Every image is an SVG in this repo, drawn by code in this
  repo.
- No invented numbers. Every figure is fetched from the GitHub API or is a curated fact from the
  portfolio and resume.

## Page structure and copy rules

Order: masthead, two-sentence intro plus two links, Play me, Hot off the press (three projects),
Hackathons (two), Open source (three), The toolbelt (toolbelt SVG, stats SVG, one certification
line), Building in private (two one-liners), Say hi.

Copy rules (from the owner's feedback and memory):

- One or two plain sentences per project that say what it does. Tech goes in inline-code tags on
  the next line, at most five.
- Awards on their own bold line under the heading, never in a trailing parenthetical.
- Team work uses "we"; the owner's own part uses "I".
- No em-dashes in sentences. Comic labels use middle dots.
- No test-count stacking, no "this shows I can X" closers, no tip or note callouts, no
  collapsible implementation tables.
- Only claims backed by the public repos or the portfolio. Details that live only in private code
  (Courtside's veto list, StormLink's FEMA and A2A mentions) are dropped.
- ClawWorld is not on the page.

## Assets (all generated by `comic/`, committed)

Fonts: `fonts/Bangers-Regular.woff2` and `fonts/JetBrainsMono.woff2` (Google Fonts latin subsets,
SIL OFL 1.1, license texts alongside). Each SVG embeds both as base64 `@font-face` so it renders
inside `<img>` through GitHub's image proxy, which allows no external fetches.

Assets are drawn at 880 px wide so they render close to 1:1 in GitHub's ~830 px README column.

- `assets/masthead.svg` (880x300): halftone paper, diagonal red-deep ink split on the right with
  speed lines, dateline `ISSUE #27 · GITHUB PROFILE · NEW BRUNSWICK, NJ`, "THE INCREDIBLE" over
  "RONIL BASU" in Bangers with the portfolio's double shadow, three chips (`DATA SCIENCE × AI`,
  `RUTGERS '27`, `BUILD · SHIP · REPEAT`), yellow burst reading `NOW WITH / A GAME / SCROLL DOWN`.
- `assets/toolbelt.svg`: four comic panels in a 2x2 grid, title bands red / blue / yellow / ink,
  chips from `data/toolbelt.json` (the resume's four groups, verbatim from the portfolio).
- `assets/stats.svg`: four panels. Public repos and contributions in the past year come from the
  GitHub API nightly; hackathon awards (2) and AI roles (3) are curated facts in
  `data/stats.json`. If the API call fails the previous numbers stay.

Shared drawing primitives live in `comic/draw.py`: palette, font CSS, halftone patterns, panel,
chip, shadowed text, star polygon, text width estimate.

## The game

### Rules and state

Standard Connect Four, 7 columns by 6 rows, four in a row wins, full board draws. Visitors are red
and always move first in a new game; the bot is blue. The board is shared: any visitor may play
the next red move.

`game/state.json`:

```json
{
  "game_no": 1,
  "board": ["......." , "......." , "......." , "......." , "......." , "......."],
  "to_play": "R",
  "moves": [{"col": 3, "by": "octocat", "issue": 12, "actor": "R"}],
  "finished": false,
  "result": null,
  "last_bot": {"col": 3, "depth": 9, "nodes": 41213, "seconds": 0.8},
  "record": {"humans": 0, "bot": 0, "draws": 0},
  "last_game": null,
  "movers": {"octocat": 1},
  "revision": 1
}
```

`board` is top row first. `result` is `"R"`, `"B"`, or `"draw"` when finished. `revision`
increments on every accepted move and is the cache-buster for the board image. `movers` counts
accepted human moves per login for a most-moves line. `last_game` stores `{game_no, result,
winning_move_by, moves}` for the previous game.

### A move

1. Trigger: an issue is opened whose title matches `^c4\|drop\|([1-7])$`. Anything else exits
   without commenting.
2. If `finished` is true, start a new game (`game_no + 1`, empty board, red to play) before
   applying the move.
3. Apply the red drop. A full column closes the issue with a one-line explanation and changes
   nothing.
4. If the game is not over, the bot picks a column and drops blue. Depth, nodes, and seconds are
   recorded.
5. If the game is over after either drop, update `record`, `last_game`, set `finished`.
6. Render `game/board.svg`, rewrite the README region, bump `revision`, commit `game/state.json`,
   `game/board.svg`, `README.md` as `github-actions[bot]`, close the issue with a comment naming
   the result and linking back to the profile.

### The bot

`connect4/bot.py`: negamax with alpha-beta pruning over bitboards (one 49-bit integer per side,
seven 7-bit columns). Iterative deepening from depth 1 with a wall-clock budget of 2.0 seconds and
a hard cap of depth 12; the last completed depth is reported. Move ordering: center first, then
outward. Terminal scores prefer faster wins and slower losses. Static evaluation counts windows
of four holding three own pieces and one empty (6 each), subtracts the opponent's (7 each), and
adds 3 per center-column piece, all computed with bitboard shifts. A transposition table keyed on
the position bitboards is cleared per move. Pure Python, no dependencies.

### Rendering

`connect4/render.py` draws `game/board-<rev>.svg` (720x740): red title band `CONNECT FOUR` with a
chip `YOU: RED · BOT: BLUE`, a paper-2 grid that runs from panel edge to panel edge with 42 holes
(cell pitch 100, so column c is centred at 60 + 100c), red and blue discs with ink outlines and a
paper glint, a yellow ring on the last disc played, and a status strip:
left `MOVE 09 · RED TO PLAY` (or `GAME OVER · RED WINS` / `BLUE WINS` / `DRAW`), right `BOT ·
NEGAMAX · DEPTH 9 · 0.8 S` from the last bot move. The two labels together stay under 56
characters so they never read as one line (the embedded font has no Greek, so no α-β).

### README region

Between `<!-- c4:start -->` and `<!-- c4:end -->` the workflow writes:

```
Red is you. Tap a column to drop.

<a href="issue link 1"><img src="assets/drop-1.svg" width="14%" alt="Drop in column 1"></a>…<a href="issue link 7"><img src="assets/drop-7.svg" width="14%" alt="Drop in column 7"></a>
<img src="game/board-42.svg" alt="Connect Four, game 3, move 9, red to play" width="100%">

Red to play · Last move by @octocat · Humans 3, bot 5, draws 1
```

The seven drop buttons (`assets/drop-1.svg` to `drop-7.svg`, drawn by `comic/buttons.py`: a yellow
chip, a Bangers digit with a red offset, a red down arrow, Bangers only so each stays near 32 KB)
are the tap targets. They sit on one line with no whitespace between the anchors, in the same
paragraph as the board, so the board renders directly under them at every viewport width. A plain
image is not tappable on the GitHub mobile app, which is why the digits moved out of the board and
into linked buttons (RON2K, 2026-09-17: "i can't even click on the game").

When finished: `Red wins, game 7 · Next drop starts game 8 · Humans 4, bot 5, draws 1`. Below the
region, one static line: `Pick a column, press Submit on the issue it opens, refresh in a minute.
Engine in [connect4/](connect4/).`

Issue link: `https://github.com/ron2k1/ron2k1/issues/new?title=c4%7Cdrop%7C3&body=Press+Submit+new+issue.+The+bot+answers+here+within+a+minute.`

Cache-busting: the board is written as `game/board-<rev>.svg` and the previous revision's file is
deleted in the same commit. A new path is the one cache key every layer (GitHub's raw redirect,
its CDN, and the camo proxy) respects; a `?rev=N` query was rejected in review because the raw
redirect can drop it.

### Workflows

- `.github/workflows/connect4.yml`: `on: issues: [opened]`, `permissions: {contents: write,
  issues: write}`, a job condition on the title prefix so unrelated issues never enter the
  job-level `concurrency: {group: connect4, cancel-in-progress: false}`, ubuntu-latest,
  `actions/checkout` on `main`, `actions/setup-python` 3.12, then three steps:
  `python -m connect4 drain --outcomes $RUNNER_TEMP/outcomes.json` (lists open move issues
  through `gh`, applies each in number order, writes state, board and README, records one
  outcome per issue), commit and push with an explicit success flag (the step fails if three
  pushes fail), and `python -m connect4 close --outcomes ...` which closes each issue with its
  result. Titles are matched in Python, never in a shell.
- `.github/workflows/stats.yml`: `schedule: cron "17 6 * * *"` plus `workflow_dispatch`, runs
  `python -m comic.stats --token "$GITHUB_TOKEN"` and commits `assets/stats.svg` and
  `data/stats.json` only if changed.
- `.github/workflows/tests.yml`: pytest on push and pull_request.

Commits from `GITHUB_TOKEN` do not trigger other workflows, which is what we want.

### Abuse and failure

- Only `issues.opened` with a matching title does anything; the job condition rejects the rest.
- Simultaneous moves: the job-level concurrency group runs one move job at a time, but GitHub
  keeps at most one run pending per group and evicts it when another arrives. So every run drains
  all open move issues in number order (`gh issue list`, filtered by the title regex) instead of
  serving only its own trigger; whichever run survives plays the evicted run's issue too.
- Issues are closed only after the push succeeded. If the push fails three times the step fails,
  the issues stay open, and the next run replays them from the state on `main`.
- A bad column, a full column, or a race that made the move illegal closes the issue with one
  line and leaves the state untouched.
- If the workflow is down the page still shows the last board and the links still work; issues
  simply queue.

## Repo layout

```
README.md
assets/masthead.svg  assets/toolbelt.svg  assets/stats.svg
comic/__init__.py  comic/draw.py  comic/masthead.py  comic/toolbelt.py  comic/stats.py
connect4/__init__.py  connect4/engine.py  connect4/bot.py  connect4/render.py
connect4/readme.py  connect4/__main__.py
data/toolbelt.json  data/stats.json
docs/superpowers/specs/…  docs/superpowers/plans/…
fonts/Bangers-Regular.woff2  fonts/JetBrainsMono.woff2  fonts/OFL-*.txt  fonts/README.md
game/state.json  game/board.svg
tests/test_engine.py  tests/test_bot.py  tests/test_render.py  tests/test_readme.py
tests/test_comic.py
.github/workflows/connect4.yml  stats.yml  tests.yml
```

## Testing

Pure Python, pytest, no dependencies. Engine: drops, gravity, full column, all four win
directions, draw. Bot: takes an immediate win, blocks an immediate loss, never plays a full column,
respects the time budget, reports depth. Render: valid XML, 42 holes, last-move ring present,
status text matches state. README: region replaced idempotently, links well-formed, text outside
the region untouched. Comic: each asset is valid SVG with both fonts embedded and expected labels.

## Verification before merge

1. `pytest` green locally and in CI.
2. Render the README through `gh api markdown` plus Playwright (light and dark): four images, no
   broken images, zero `[align="center"]`.
3. Push the branch, open the `/tree/feat/comic-readme-connect4` page in a real browser: all four
   SVGs render with the comic fonts, the board has the yellow ring, and the `?rev=` query survives
   on the resolved image URL.
4. Play one move against the bot on the branch via a real issue after merge, and confirm the
   board redraws within two minutes. The workflow only runs from `main`, so this is the last
   check.
5. Review by the code-reviewer agent before the PR is opened.
